{
  "name": "parsers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chainmail parsers — private, NOT open-source. Parses CEX/DEX/NFT/explorer emails into structured Receipts.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "pnpm build && node --test tests/parsers.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.10.0"
  }
}
